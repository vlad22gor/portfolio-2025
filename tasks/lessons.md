# Lessons

- Держать заметки краткими, без дублей и противоречий.
- Для сложных scallop-форм использовать quantized SVG-компонент вместо mask-пересечений.
- Для Figma handoff scallop: `pad` брать из padding контейнера, `shape` выводить из corner radius (`9999` -> `circle`, иначе `rectangle`).
- Для wave-divider использовать SVG stroke из чередующихся полуокружностей (`d=8`, `stroke=2`), длину квантизировать только количеством кружков без mask.
- Для текущего этапа вёрстки: desktop-only, один рабочий breakpoint `1360`; mobile breakpoint `360` добавляется отдельной задачей.
- Горизонтальная страничная сетка в коде: `main.page-shell` по умолчанию `5-col` (`144/24`, track `816`), для `/gallery` использовать модификатор `page-shell--gallery` с `8-col` (`132/24`, track `1224`); общий вертикальный `gap`-ритм не вводить.
- Для `/gallery` (rows `51:5306`) держать контракт `6` рядов с `row-gap: 120`, карточка `height: 384`; `phone/tablet/illustration` — `span 2` через `QuantizedPerimeter edgePattern='scallop' step=48`, `image` — `span 4` через `QuantizedPerimeter edgePattern='totem' step=50` с общей маской `--perimeter-content-mask` и для image-слоя, и для darkened bg-слоя.
- Для `/gallery` у второй `image`-карточки (`57:5450`) использовать подготовленный ассет `cube log in` из `assets/gallery/static/` через runtime-копию `public/media/gallery/images/r5-c3-cube-log-in.webp`.
- Для `/gallery` карточку `51:5287` (`cube`) рендерить как `loader-light.webm` (`autoplay + loop + muted + playsinline`), а `cube.webp` использовать как `poster`/fallback.
- Для `/gallery` карточку `51:5274` (`coin-wheel`) держать в типе `illustration`, но контент рендерить как `coin-wheel.webm` (`autoplay + loop + muted + playsinline`) через runtime-путь `public/media/gallery/illustrations/coin-wheel.webm`; `coin-wheel.webp` использовать как `poster`.
- Для `/gallery` держать порядок и Y-контракт секций из `51:5014`: `rows(240) -> to be more(3240, 144x144, circle D=24) -> final cta(3576)` и отступ к `footer(4656)`; в `page-shell` (`gap: 32`, `padding-bottom: 48`) это даёт `to-be-more margin-top: 64`, `final-cta margin-top: 160`, `final-cta margin-bottom: 138`.
- Для `/gallery` блока `to be more` переносы из Figma (`Shift+Enter`) фиксировать явным `<br>` в разметке; актуальный copy: `there will` + `be much more joyfull works!`.
- Вертикальные отступы секций задавать по `Y` из Figma (а не по единому `gap`): для первого блока `home hero` базовый контракт `Y=240`.
- Если `SiteHeader` нефиксированный (`position: static`), для сохранения контракта `home hero Y=240` компенсировать высоту хедера в `page-shell--home`: `padding-top: calc(240px - var(--site-header-height))`.
- Для `cases cards section` на `/` держать desktop-контракт `Y=1320`; в description-блоке стрелки фиксировать по Figma (`left:155, top:-12` и `left:604, top:32`).
- Для `design tools section` на `/` держать desktop-контракт `Y=3024`: `label row` выравнивать по правому краю 5-колоночной сетки (`144/24`), divider между рядами — `QuantizedWave size='small' fit='cover' align='right'` в span `1->5` колонок справа налево (`5/6`, `4/6`, `3/6`, `2/6`, `1/6`) через отдельные классы `design-tools-wave-span-*` (не переиспользовать `design-tools-col-*`); длину считать по ширине собственного span (убираем лишний кружок через `ceil(width / d) - 1`, без scale-деформации path).
- Для `/fora intro` divider `QuantizedWave size='medium'` длину ограничивать count-сегментами (`312 -> 22`, `144 -> 10`, `312 -> 22`), не через `overflow: hidden`/clip.
- Для `/fora intro` тексты (`overview/scope/results`) синхронизировать 1:1 с Figma `37:4182`; при переносе исправлять только явные опечатки/регистровые неточности без изменения смысла.
- Для `/fora intro` колонки в нижнем ряду держать с top-align на уровне контейнера (`.fora-intro-bottom { align-items: flex-start; }`), чтобы `results` не смещался вниз при разной высоте контента.
- Для `/fora process` тикеты рендерить через `QuantizedPerimeter` с `edgePattern='scallop'`, `step=36`, `pad=0` и фиксированным размером `144x144`; ширину тикета задавать селектором с большей специфичностью, чем базовый `:is(.quantized-scallop,.quantized-perimeter){width:100%}`.
- Для `case process tickets` использовать параметр `ticketVariant`: `square-36` (default, `shape='rectangle'`, `step=36`) и `circle-24` (`shape='circle'`, `step=24`); для `kissa` задавать `ticketVariant='circle-24'`, сохраняя внешний размер тикета `144x144`.
- Для `case process tickets` внутренний inset задавать через `.case-process-section .case-process-ticket .scallop-content { padding: 20px; box-sizing: border-box; }`, а текст — `width: 100%` (без фиксированного `110px`), чтобы фактический отступ был ровно `20px` и не перебивался глобальным `--scallop-pad`.
- Для текстов `case process tickets` применять гибридный рендер: хранить ручные переносы в данных как `\n`, в компоненте преобразовывать их в явные `<br>`, а `.case-process-ticket__text` держать с `white-space: normal` для авто-wrap внутри строк; неразрывные пробелы фиксировать явно как `\u00A0`.
- Для `/fora feature cards` использовать reusable `FeatureCard` с props `mockSide ('left'|'right')` и `device ('phone'|'tablet')`; секционный контракт: `816x1580`, card `432`, вертикальный gap `142`; mock-контейнер на `QuantizedPerimeter step=48`, видео/постеры — из `public/media/cases/fora/feature-cards/{flows,posters}`.
- Для `DeviceMockup tablet` (Figma `43:1811`) держать `Screen` под `Shell-tablet` (`z-index`), без `border-radius`; inset экрана задавать в процентах от базового `296x508` (эквивалент `11.5px`) для корректного масштабирования.
- Для overlap-сценариев `DeviceMockup` с внутренними `z-index` (`screen/shell`) обязательно изолировать стек компонента (`.device-mockup { isolation: isolate; }`), чтобы слои разных mockup не перемешивались между собой.
- Для `phone`-мокапов радиус экрана задавать централизованно в `DeviceMockup` (`screen.radius`), а секционные страницы (включая `/gallery`) не должны дублировать/переопределять этот радиус локальными селекторами.
- Для `/gallery` использовать `DeviceMockup size='compact'` (без `scale`) для предсказуемой геометрии: `phone 216x443`, `tablet 258x443` по Figma `43:1799`.
- Для `DeviceMockup phone + compact` геометрию `screen` калибровать по AA-aware aperture shell (inset-ratios `50/976`, `48/976`, `43/2004`, `43/2004`) и помечать режим `data-device-screen-calibration='aperture-compact-v2-aa'`.
- Для video-screen в `DeviceMockup` anti-seam делать через отдельный bleed-wrapper (`top/right/bottom/left: -1px`, `overflow: hidden`) и `video { inset: 0; transform: none; }`; не использовать `transform-scale` для антишва.
- Для `DeviceMockup` в `size='compact'` использовать `shell` с `object-fit: fill`, чтобы избежать микрокропа низа/верха от несовпадения aspect-ratio (`216x443` vs `976x2004`).
- Для `DeviceMockup` масштаб применять через единый `--device-scale` ко всей геометрии (`frame + screen`): при переданном prop `scale` он имеет приоритет (inline), без prop компонент не должен инлайнить дефолт `1`, чтобы секции могли безопасно переопределять scale через CSS.
- Для `/fora team photo section` (`49:2607`) держать контракт `816x500`: photo-блок `816x432` через `QuantizedPerimeter` (`edgePattern='scallop'`, `step=48`, `clipContentToShape=true`), подпись `297x44`, hearts в абсолюте (`right: 552/451, 36x28`; `left: 240/486, 34x29`).
- Для `/` держать порядок секций и Y-контракты как в Figma: `hero(240) -> cases(1320) -> design tools(3024) -> about me(3744) -> quotes(5112) -> final cta(6000)`, затем `footer(7080)`; при расчёте `margin-top` учитывать `main.page-shell` `grid gap` (`32px`).
- Для `final cta` на `/`: divider рендерить full-bleed на ширину viewport (`left=0/right=viewport`) отдельным слоем, не задавать собственный фон секции, а motif использовать в синем варианте (`/media/motifs/motif-stack-orb-3-blue.svg`).
- Для desktop-навигации при route-transition резервировать scrollbar gutter на root (`scrollbar-gutter: stable both-edges`, fallback `overflow-y: scroll`), чтобы избежать горизонтального layout shift fixed-header и контента.
- Никогда не ссылаться на `assets/` из runtime-кода: `assets/` — только источник исходников; используемые ассеты переносить в проектные runtime-пути (`public/media/...` или импортируемые файлы в `src`) и подключать только оттуда.
- Для `header-button`: фиксированная высота `40`, top-align контента, `label container` с `padding-inline: 8px` и hug-width; у `selected` hover-анимация `gap` от `3` до `8` через motion spring. Для `wave-rail` (дефолт): рендерить одну общую `QuantizedWave` и показывать только активный сегмент через `path trim` (`stroke-dasharray`/`stroke-dashoffset`); ширина trim-сегмента считается от `label container` по правилу `ceil(width / d) - 1` (не уже `1` кружка), а сама общая rail-волна должна покрывать всю ширину трека (`cover-full`: `ceil(width / d)` без `-1`) для корректной центровки крайних табов.
- Для `wave-rail` при статичном `SiteHeader` fallback-волна активной кнопки должна быть вне потока: у `data-wave-variant='wave-rail'` держать `--header-button-gap: 0` всегда, контейнер fallback-волны активной кнопки `height: 0`, а в `data-wave-rail-ready='true'` переключать только `opacity`, чтобы не было layout shift на reload.
- Для `wave-rail` fallback-волну активной кнопки показывать только в pre-ready фазе (`:not([data-wave-rail-ready='true'])`); в `ready=true` принудительно скрывать fallback c равной/большей специфичностью (`opacity: 0` + `visibility: hidden`), чтобы не появлялась «вторая волна».
- Для `wave-rail` trim-пересчёта избегать layout-чтений в каждом кадре: кэшировать rail-метрики (`ratio/offset/totalLength`) и обновлять их только на init/resize/`astro:page-load` или когда `data-wave-rail-ready=false`; при совпадении optimistic target с фактическим active на `astro:page-load` не перезапускать анимацию.
- Для выраженного bounce на крайних табах в `wave-rail` использовать симметричный overscan path (`fit='cover-bleed'`, `bleedCircles=2`): волна становится длиннее и сдвигается влево на `bleed*d`, чтобы справа/слева оставалось равное пространство для overshoot без дрейфа центра сегмента.
- В `wave-rail` hover-эффект лучше делать не через `gap` кнопки (он принудительно обнулён), а через смещение общей rail-волны CSS-переменной `--wave-rail-hover-offset`; для дизайн-компенсации можно добавить базовый сдвиг `--wave-rail-base-offset` (например, `-4`) и суммировать его с hover-offset в `transform`, а `hover-offset` сбрасывать в `sync()/astro:page-load`.
- Для persisted-header при soft navigation: для hover в `wave-rail` считать активность только по route-state (`aria-current='page'` -> `.active`) и не опираться на optimistic `data-wave-active-id`; `data-wave-active-id` использовать для trim/reconcile. После первого успешного trim держать `data-wave-rail-ready='true'` sticky, чтобы не показывать fallback-волну при кратковременной неготовности метрик.
- Для длинных `wave-rail` tab-hop использовать адаптивный delay-gate навигации `120..320ms` (по доле пути сегмента) и не стартовать `navigate()` по `RAF`; для сглаживания soft-nav включать глобальный prefetch (`prefetchAll + defaultStrategy='load'`), а для кейс-деталок ставить `data-astro-prefetch='false'`.
- Для `QScallop`/периметров использовать `QuantizedPerimeter` как runtime-движок (а `QuantizedScallop` — только wrapper для совместимости); квантацию вести по inside-fit: размер не растёт за дизайн-контейнер и округляется вниз.
- Для all-edges `QuantizedPerimeter` с `step=48` фиксированные секции нужно задавать полным контрактом `height + min-height + max-height` (или `min==max`) и высотой, кратной шагу; иначе возможны runaway/snapping-деформации. Для `about` использовать `1152` и компенсировать downstream-отступ на `-1px`.
- Для fixed-size `QuantizedPerimeter` не использовать `%`-контракт (`min/max-height: 100%`) на самом root-периметре: это может зафиксировать ранний неверный budget и растянуть `viewBox`; для intro surfaces задавать явные `px` (`432/432/432`).
- Для rectangle-режима `QuantizedPerimeter` budget геометрии брать из размеров контейнера без `content.scrollWidth/scrollHeight`: при `fixed` блоках (`min==max`) приоритет у `root`, иначе fallback `parent -> root -> step`; `scroll*` допустим только для флага overflow.
- Исключение для footer: у `QScallop` использовать квантацию только по верхней грани (`rectangleEdges="top"`), нижняя и боковые грани остаются прямыми; допускается асимметричный inside-fit padding `84/72` (top/bottom), а фоновую заливку для боковых зазоров делать через `.site-footer::before` (top offset = scallop radius); motifs остаются декоративными (`aria-hidden`) и анимируются только по `pointer-hover` с profile-based spring (`slow`: `1.35/0.25`, `fast`: `1.05/0.25`).
- Для Astro `ClientRouter` любые DOM-runtime скрипты (`QuantizedWave/Perimeter`, интеракции навигации) должны быть идемпотентными и переинициализироваться на `astro:page-load`; на page-load монтировать только новые swapped-узлы (без глобального `scheduleAll()`), иначе возможен jank в анимации persisted-хедера.
- Для `home hero` при soft-nav на `/` использовать pre-arm state-машину `data-home-hero-appear='pending|running|done'`: initial-кадр задавать в CSS на `pending`, в runtime анимировать `0->1` и завершать в `done`; для `no-js` обязательно держать `noscript` override, чтобы `pending` не скрывал контент.
- Для scroll-enter анимаций секций/блоков использовать единый глобальный паттерн `data-motion-inview='appear-v1'` (one-shot): `opacity 0->1`, `translateY 50->0`, `ease [0.44,0,0.56,1]`, `duration 0.4`, `amount: 0`; локальные override параметров не допускаются.
- Для staged-reveal использовать двухскоростной контракт: `appear-stagger-v1` = default `childDelay 0.1`, `appear-stagger-dynamic-v1` = dynamic `childDelay 0.08` (остальной motion-контракт одинаковый).
- В case-details (`/fora`, `/kissa`) dynamic (`appear-stagger-dynamic-v1`) применять только в `intro` и `artifact photos`; `intro screens`, `challenge`, `process`, `feature cards` держать на обычном `appear-v1`/базовом in-view без stage-индексов.
- Для `CaseProcessSection` у `.case-process-section__tickets-row` использовать `process-tickets-row-stagger-dynamic-v1` (`childDelay 0.08`), а в `/gallery` сохранять `process-tickets-row-stagger-v1` (`childDelay 0.05`).
- Для `cases cards section` на `/` staged reveal строить на `.cases-cards-list` через `appear-stagger-v1` с индексами `0/1/2` (`fora card -> description -> kissa card`), без локального `inView` у `CaseCard` и `description`; у стрелок `cases-cards-description` сохранять отдельные пресеты `cases-arrow-left-v1` и `cases-arrow-right-v1`.
- Для side-стрелок внутри `appear-stagger-v1`/`appear-stagger-dynamic-v1` использовать `data-motion-stage-side='left|right'`: `left => offsetX 25`, `right => offsetX -25`, оба с `scale 0.9->1`, `opacity 0->1` и без `translateY` (актуально для `artifact photos`).
- Для `design tools` bottom labels использовать отдельный `inView`-пресет `design-tools-label-char-v1`: побуквенно (`opacity 0->1`, `rotate 45->0`), spring `0.45/0.25`, char delay `0.06`; row-stagger задаётся через `data-motion-row-index` (`+0.1` слева направо в пределах row), пробелы не анимируются как отдельные символы.
- Для `about` arch использовать отдельный `inView`-пресет `about-arch-trim-v1`: trim path `start->end` на inline SVG-path (`data-motion-trim-path`) с визуальным направлением слева направо (L->R, path start слева), spring `0.6/0.3`, trigger `amount: 0.3` (старт при ~30% видимости), one-shot, reduced-motion сразу в финал.
- Для main quote в `quotes section` использовать sequence-паттерн: текст `quotes-main-word-v1` (per-word `opacity 0->1`, `y 10->0`, spring `0.4/0`, stagger `0.075`) + закрывающая кавычка `quotes-main-close-after-v1`, которая стартует только после завершения анимации текста.
- Для `final cta` использовать отдельный repeatable `inView`-пресет `final-cta-morph-v1` на root-секции (`amount: 0.7`, `type:'spring'`, spring `0.6/0.1`): анимировать только `title + motif` через CSS vars (`title gap 350->22`, `title shiftX 4->0`, `orb offsetY -10->0`, `orb rotate 90->0`) и триггерить по одной viewport-линии `window.innerHeight * (1 - amount)` (для `0.7` это `30%` сверху): вниз (`top > line -> top <= line`) = `final`, вверх (обратное пересечение) = `initial`, без `intersectionRatio`-leave триггеров.
- Для `inView`-runtime при анимации `transform` всегда композировать `base transform + motion transform` (а не перетирать `transform` inline), иначе ломается геометрия элементов с собственным позиционирующим transform (например `final-cta-divider-bleed` с `translateX(-50%)`).
- Для sans-типографики использовать variable `DM Sans` через axis-токены (`opsz`, `wght`), а отрицательный кернинг заголовков `t1`/`t1-tight`/`t1-compact` держать `-2px` (по Figma token spec).
- Для актуального token spec: `t5=24/29`, `t6=20/24`; в разметке использовать `type-t6`, legacy-класс `type-t5` не применять.
- Для цветовых коллекций использовать глобальный контракт `html[data-theme='light'|'dark']` с bootstrap через `localStorage('vh-theme') + prefers-color-scheme`; desktop `FloatingThemeButton` (persist) переключает тему, хранит выбор и синхронизирует `aria-pressed/aria-label`.
- Для `ClientRouter` soft-nav сохранять тему на swap-этапе: в `astro:before-swap` копировать текущий `data-theme` в `event.newDocument.documentElement`, а `FloatingThemeButton` при временно отсутствующем `data-theme` брать тему из `localStorage/cached`, чтобы исключить кратковременный откат в light.
- Компонентные коллекции `button/*`, `footer/bg`, `ink/bg`, `divider/bg` держать в полном sync 1:1 с Figma `83:19088` (light) и `83:19089` (dark); для обратной совместимости `--color-icon-accent` оставлять alias на `--color-ink-bg`.
- При синхронизации dark-коллекции `83:19089` учитывать полный пакет значений: `bg/darkened = #234978`, `button-floating/bg = #234978`, `footer/bg = #234978`, `ticket/bg/{orange,blue} = #6a9ecf/#5c8dbd/#4d7baa/#3e6998/#305885`.
- Для `Button` (`bordered`/`bordered-icon`) в default использовать `--color-button-text-outlined`, а в hover возвращать `--color-button-text`; для `footer` мотивов использовать mask-рендер (`ThemedSvgIcon`) с `--color-accent-white`, чтобы тема переключала цвет без SVG-файловых дублей.
- Для `Button` `bordered-icon`: в default держать цвет стрелки от `--color-button-bg` (контраст на dark фоне), в hover переключать на `--color-button-arrow`.
- Для всех `QuantizedWave` использовать дефолтный цвет от `--color-divider-bg` (включая header wave-rail, intro/design-tools/final-cta dividers) и не переопределять локально другим токеном без явной причины.
- Для `FloatingThemeButton` мотивы рендерить через CSS mask + токен `--color-button-floating-motif` (не через встроенный цвет SVG), `pressIn` scale держать `0.9`, а в dark применять `--floating-theme-icon-offset-x: -1.5px` для визуальной компенсации.
- Для токена `type-body` держать `paragraph spacing` как отдельный глобальный токен `--type-body-paragraph-spacing: 10px`; применять только через `.type-body p + p` (без правила `.type-body + .type-body`), чтобы не ломать top-align соседних колонок.
- Для script-токенов использовать `type-description-medium` (`20/22`) и `type-description-large` (`24/26`) с `text-transform: lowercase`; legacy `type-description` не использовать.
- Для `type-description-*` (Caveat variable) явно задавать `font-variation-settings` с нужным `wght`; одного `font-weight` недостаточно, если выше по дереву уже задан `font-variation-settings`. Для текущего визуала целевой `wght=485`.
- Для Agentation в этом проекте использовать только dev-only режим: аннотации запускаются через `npm run dev:annotate` (плюс `agentation-mcp server`), обычная разработка — `npm run dev` без toolbar.
- Для демо-блоков `QuantizedPerimeter/QuantizedScallop` цвет поверхности задавать на root через `--scallop-bg`, а `.scallop-content` держать прозрачным, чтобы не перекрывать edge-shape.
- В `QuantizedPerimeter/QuantizedScallop` не задавать theme-дефолты (`--scallop-bg`, `--scallop-pad`) inline по умолчанию; передавать их только явно, чтобы классы контейнера могли переопределять токены.
- Для `QuantizedPerimeter edgePattern='totem'` использовать C1-гладкий профиль (`1 лоб = 2 cubic`) с раздельными `edge/crest` handle-length и гибридными углами: волна идёт по укороченной грани (`cornerInset`), а переходы между гранями делать через `corner-bridge` (cubic quarter-arc), чтобы убрать вытянутые углы и сохранить симметрию подхода.
- Для `Button` из Figma `12:1029` поддерживать `default`, `bordered`, `bordered-icon` с `iconSide='left'|'right'`; `hover` делать через `opacity: 0.9`, `pressed` через spring `scale: 0.95`, а все состояния красить только компонентными токенами `--color-button-bg/text/arrow/border` (без смешивания с `accent/*`).
- В `QuantizedPerimeter` держать hover-анимацию как single-type param animation через adapter-контракт (`resolveBaseParams/resolveHoverParams/interpolateParams/render`) и не возвращаться к inter-type morph между паттернами.
- Для hover-анимаций `QuantizedPerimeter` использовать единый `hoverMorphPreset='default'|'strong'` с pattern-specific интерпретацией: у `totem` меняются `waveAmplitudeRatio/waveTension` (с optional override через `hoverWaveAmplitudeDelta/hoverWaveTensionDelta`), у `scallop` меняется внутренний `scallopRadiusScale`; пунктирную inner-обводку контентного слоя не использовать.
- Для `ClientRouter` в `dev` держать header prefetch отключенным (`data-astro-prefetch='false'`) и фиксировать критичную геометрию hero-ассетов (например `coin wheel`) inline-стилем, чтобы исключить drift из-за stale CSS при soft-nav + HMR.
- Для `CaseCard` держать контейнер cover без clip (`clipContentToShape=false`), а scallop-маску применять только к `cover-image` через экспортированный `--perimeter-content-mask`; декоративный `border-radius` у image не использовать.
- Для блоков с `QuantizedPerimeter`, где нужно управлять `.scallop-content` и маской `cover-image`, правила держать в `global.css` (или `:global(...)`): scoped-стили Astro-компонента не применяются к внутреннему DOM дочернего компонента.
- Для `CaseCard` держать layout-ориентацию как данные (`coverSide`), а не slug-условия в шаблоне; `kissa` — `coverSide='right'`.
- Для `CaseCard` корневой элемент должен быть ссылкой на кейс: кликабельна вся карточка, а не только title.
- Hover-ассеты `CaseCard` должны рендериться под слоями `cover/text` (за карточкой), а stagger задавать по вертикальному порядку `targetY` (верхний без задержки, нижний `+0.08s`).
- Для `CaseCard` держать раздельные spring-длительности: mouse-in `duration: 0.4`, mouse-out `duration: 0.6`, `bounce: 0.2`.
- Для `CaseCard` hover-ассетов в данных хранить размеры до rotation (`width/height` pre-rotation), а `targetX/targetY` — как top-left итогового позиционирования в runtime.
- Для `CaseCard` с `coverSide='right'` нормализовать `targetX` в runtime по ширине карточки: `effectiveX = rawX - max(0, designWidth - cardWidth)` (чтобы figma-координаты из wider frame не уезжали вправо).
- Для кубика в `more is coming (QScallop)` использовать `loader_light.webm` как autoplay/loop `video`, а `more-cases-cube.png` оставлять `poster` и fallback; прод-путь — `public/media/cases/section/`.
- При обновлении hover-ассетов из Figma для `CaseCard` (`kissa`) сохранять raw-координаты/размеры 1:1 из фрейма `20:1323`; компенсацию ширины выполняет runtime-нормализация `effectiveX`.
- При повторных коррекциях `kissa` сначала обновлять только raw `targetX/targetY/width/height` из Figma, а runtime-логику нормализации не трогать (она должна оставаться единственным местом компенсации).
- Для ровной scallop-обводки в `CaseCard` использовать `QuantizedPerimeter outlineMode='geometric'` + `outlinePlacement='outside'`: внешний контур строится как doubled-stroke с inverse SVG-mask; `alpha`-outline оставлять fallback для неподдержанных паттернов/форм.
- Для `Badge` (`.case-badge`) держать API `type='default'|'outlined'`: `default` в light использует tone-заливки (`blue/gray/orange/green`), а в dark принудительно рендерится как `outlined` через `html[data-theme='dark'] .case-badge[data-badge-type='default']`; explicit `type='outlined'` остаётся outlined в обеих темах. Геометрия: `height: 22px`, `box-sizing: border-box`, `padding: 0 8px`; лейбл рендерить через `.case-badge__label` без Y-offset.
- Для PNG-ассетов из Figma с тенями перед импортом в runtime проверять, что экспорт не деформирует визуальный масштаб (частая причина — некорректный bounds/обрезка при сохранении); после реэкспорта обновлять только runtime-копии в `public/media/...`, не подключая `assets/` напрямую.
- Для case-runtime ассетов использовать единый canonical namespace: `public/media/cases/<slug>/...`; смешанные пути (`/media/<slug>-*.svg`, `/media/fora/...`) не использовать.
- Для detail-страниц кейсов держать data-driven композицию в `src/data/case-details/<slug>.ts` и рендерить через общий `CaseDetailSections`; для кейсов без конфига оставлять fallback-ветку в `src/pages/[slug].astro`.
- Для video flow ассетов в CI обязательно запускать `npm run verify:posters` (валидирует пары `flows/*.webm -> posters/*.png`, размер и PNG-метаданные).
- Для lossless-оптимизации растрів использовать pipeline `assets:collect-render-sizes -> assets:validate-3x`; минимальный порог — не ниже `3x` от desktop render-size, posters в `/posters/` оставлять в PNG, остальные runtime-растры переводить в WebP lossless, если это уменьшает размер.
- Для временного mobile/tablet экрана `TemporaryAdaptiveNotice`: в `screens` использовать явный `DeviceMockup size='small'` (без `scale`), не анимировать `scale/opacity` карточек (только `translateY + rotate`), считать `translateY` по геометрии окружности и задавать дугу через breakpoint-профили (`phone/tablet`) с proportional `arcMaxDx/arcSagitta`; для `phone/small` держать `screen.radius=12`, AA-aware aperture-калибровку `aperture-small-v2-aa` с `screen` overscan `1px` и отключённый дополнительный `video-bleed` (`--video-bleed-px: 0`), чтобы не было double-overscan у `webm`; без route-specific overscan-хаков, `screens` снизу не клипать (`overflow-y: visible`).
- Для `TemporaryAdaptiveNotice` staged reveal делать фиксированным preset `temporary-adaptive-stagger-v1` на контейнере контента: child-selector `[data-temp-stage-item]`, шаги строго `screens/title/subtitle/button` с индексами `0/1/2/3` и delay-step `0.1`; motion-параметры детей должны оставаться в контракте `appear-v1` (`opacity 0->1`, `translateY 50->0`, `duration 0.4`, `ease [0.44,0,0.56,1]`).
- Для глобального social preview использовать runtime-путь `public/media/site/site-cover.png`; при замене исходника из `assets` синхронизировать `og:image:width/height` в `BaseLayout` с фактическим размером файла.
- При глобализации `<title>` в `BaseLayout` синхронизировать smoke-тесты (`toHaveTitle`) для `/gallery` и case-detail страниц, иначе CI падает на устаревших page-specific ожиданиях.
- Для устранения микровспышки на soft-nav в Astro держать policy «fade только в контенте»: `site-header/site-footer` с `transition:animate='none'`, `root`-transition отключать (`::view-transition-*(root)`), а `page-content/gallery-content` анимировать кастомными keyframes без `mix-blend-mode: plus-lighter` (включая fallback-ветку `data-astro-transition-fallback`).
- Для переходов case-switcher (`/fora <-> /kissa`) route-transition gate по marker `__case-switcher-intro-sync` применять и к `stagger` (`intro`), и к `element` (`intro screens`) через `data-motion-await-route-transition='case-switcher-intro'`; старт анимации только после стабильного снятия `html[data-astro-transition]`.
- Для `FloatingThemeButton` использовать единый `toggle`-пресет (без разделения `light/dark`) и подстраивать характер только параметрами слоёв.
- Для оперативной подстройки loudness у текущего `FloatingThemeButton`-звука сначала менять только `masterGainValue` в `theme-toggle-sound.ts`, не трогая форму пресета.
- Для направления «живой реальный выключатель» держать короткий фильтрованный `noise`-транзиент плюс компактное осцилляторное тело; это дает реализм и сохраняет читаемый щелчок.
- Для более резкого и звонкого щелчка поднимать верхний `bandpass`-слой (`filterFrequency/Q`) и держать короткий `decay` (~10-20ms), избегая выраженного «удара».
- Для уменьшения глухоты поднимать `lowpass` cutoff у body-слоёв и сохранять короткий хвост без затяжного low-mid резонанса.
