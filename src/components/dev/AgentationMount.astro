---
import AgentationToolbar from './AgentationToolbar';

interface Props {
  endpoint: string;
}

const { endpoint } = Astro.props;
---

<AgentationToolbar client:only="react" endpoint={endpoint} />
